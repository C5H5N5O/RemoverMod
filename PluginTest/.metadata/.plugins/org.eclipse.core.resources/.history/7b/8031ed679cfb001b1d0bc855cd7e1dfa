package get_xyz.main;

import org.bukkit.entity.Player;

public class Sorter {
	
	BlockFinder BF = new BlockFinder();
	
	public Player[] GetPLayersCanListen(Player player, Player[] players, Player[] AlreadyChecked) {
		Player[] PlayersCanListen = {player};

		for (Player p : players) {
			if (p.getLocation().getBlockX() - p.getLocation().getBlockZ()) {
				
			}
		}
		
		return PlayersCanListen;
	}
	
	public Player[] GetSortedPLayers(Player[] players) {
		Player[] Sortedplayers = {};
		
		// wer123 wer123
		return Sortedplayers;
	}
}
